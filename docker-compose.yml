services:
  gitstat:
    build: .
    ports:
      - "12580:12580"
    volumes:
      - gitstat-data:/home/gitstat/.gitstat
      - ./.gitstat-gitee-cache:/home/gitstat/.gitstat-gitee-cache
    environment:
      - GITEE_TOKEN=${GITEE_TOKEN:-}
    restart: unless-stopped

volumes:
  gitstat-data: